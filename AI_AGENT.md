NOTE!! In this branch, dont include ai and internal package as its not for this branch. dont read it.

In XMC package, I want to add new `search` module.

add this C:\Users\azla\Downloads\get_config_openapi (1).json into the schema called `search` (rename the file)

generate the client, for both experimental and non experimental 

${this.edgePlatformProxyUrl}/search/