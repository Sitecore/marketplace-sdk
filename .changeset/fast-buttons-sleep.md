---
'@sitecore-marketplace-sdk/xmc': major
---

Update Search API schema and response models. Remove legacy Config fields, add new configuration models and required fields, tighten Field and SearchFeature types, type error payloads as unknown, and add the sourceType enum.
